###############################################################################
###############################################################################
###                                                                         ###
###                    GENERATED BY PARTIAL DOCKERFILE                      ###
###                    NOTE: DO NOT UPDATE MANUALLY                         ###
###                                                                         ###
###############################################################################
###############################################################################
FROM centos:7

RUN yum install -y \
 wget gcc make \
 && rm -rf /var/cache/yum

RUN yum install -y \
 zlib-devel openssl-devel libffi-devel \
 sqlite-devel readline-devel \
 && rm -rf /var/cache/yum

RUN wget https://www.python.org/ftp/python/3.7.1/Python-3.7.1.tgz -O python.tgz \
 && tar -xvf python.tgz \
 && mv Python-* python \
 && ( \
  cd python \
  && ./configure \
  && make \
  && make install \
 ) \
 && rm -rf python*

RUN curl https://bootstrap.pypa.io/get-pip.py | python

RUN python3 -m venv venv
ENV PATH=/venv/bin:$PATH

CMD python